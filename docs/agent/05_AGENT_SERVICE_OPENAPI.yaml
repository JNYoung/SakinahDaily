openapi: 3.1.0
info:
  title: Sakinah Content Agent
  version: 0.1.0
paths:
  /agent-runs:
    post:
      summary: Create an agent run
    get:
      summary: List agent runs
  /agent-runs/{run_id}:
    get:
      summary: Get an agent run
  /agent-runs/{run_id}/review-packet:
    get:
      summary: Get review packet
  /agent-candidates/{candidate_id}/qa:
    post:
      summary: Run QA
  /agent-candidates/{candidate_id}/promote-to-cms-draft:
    post:
      summary: Promote candidate to CMS draft
